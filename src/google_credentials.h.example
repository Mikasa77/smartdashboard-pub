/*
 * google_credentials.h — git-ignored; copy from .example and fill in your values.
 *
 * GCP setup: https://console.cloud.google.com
 *   1. Create a project, enable Google Calendar API
 *   2. Create OAuth2 credentials — type: "TV and Limited Input Devices"
 *   3. Copy client_id and client_secret below
 */
#pragma once

#define GOOGLE_CLIENT_ID     "YOUR_CLIENT_ID.apps.googleusercontent.com"
#define GOOGLE_CLIENT_SECRET "YOUR_CLIENT_SECRET"
